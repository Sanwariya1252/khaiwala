import 'package:flutter/material.dart';

class RulesRates extends StatelessWidget {
  const RulesRates({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      
    );
  }
}