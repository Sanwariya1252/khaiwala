import 'package:flutter/material.dart';

class SharePoint extends StatelessWidget {
  const SharePoint({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      
    );
  }
}