import 'package:flutter/material.dart';

class WalletPage extends StatelessWidget {
  const WalletPage({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Wallet')),
      body: const Center(child: Text('Wallet Page')),
    );
  }
}

class PassbookPage extends StatelessWidget {
  const PassbookPage({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Passbook')),
      body: const Center(child: Text('Passbook Page')),
    );
  }
}

class BankAccountsPage extends StatelessWidget {
  const BankAccountsPage({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Bank Accounts')),
      body: const Center(child: Text('Bank Accounts Page')),
    );
  }
}

class RulesRatesPage extends StatelessWidget {
  const RulesRatesPage({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Rules & Rates')),
      body: const Center(child: Text('Rules & Rates Page')),
    );
  }
}

class MyBidHistoryPage extends StatelessWidget {
  const MyBidHistoryPage({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('My Bid History')),
      body: const Center(child: Text('My Bid History Page')),
    );
  }
}

class MyWinHistoryPage extends StatelessWidget {
  const MyWinHistoryPage({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('My Win History')),
      body: const Center(child: Text('My Win History Page')),
    );
  }
}

class ResultChartsPage extends StatelessWidget {
  const ResultChartsPage({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Result Charts')),
      body: const Center(child: Text('Result Charts Page')),
    );
  }
}

class ReferAppPage extends StatelessWidget {
  const ReferAppPage({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Refer App')),
      body: const Center(child: Text('Refer App Page')),
    );
  }
}

class SharePointsPage extends StatelessWidget {
  const SharePointsPage({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Share Points')),
      body: const Center(child: Text('Share Points Page')),
    );
  }
}

class UpdatePasswordPage extends StatelessWidget {
  const UpdatePasswordPage({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Update Password')),
      body: const Center(child: Text('Update Password Page')),
    );
  }
}

class LanguageChangePage extends StatelessWidget {
  const LanguageChangePage({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Language Change')),
      body: const Center(child: Text('Language Change Page')),
    );
  }
}

class ContactUsPage extends StatelessWidget {
  const ContactUsPage({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Contact Us')),
      body: const Center(child: Text('Contact Us Page')),
    );
  }
}

class LoginPage extends StatelessWidget {
  const LoginPage({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: const Text('Login Page'),
      ),
    );
  }
}
